version: '3.8'
services:
  db:
    image: postgres:13.7
    container_name: db_docker
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=docker
    ports:
      - 5432:5432
